# ЖУРНАЛ изменений — подпроект «02_разработчик»

Правила работы: блочная работа (Шаблон 5), регрессия после каждой правки
(правила 19–20), снапшоты перед авторегенерацией, синхронизация канон↔глобальная
папка по SHA-256. Полный свод — `00_ПРАВИЛА_ПРОЕКТА.md` (в корне проекта).

Источник вдохновения/эталон: подпроект `01_учитель` (промт v6, 6 скилов,
механика регрессии, чек-листы).

## Дата: 2026-09-20

### ФАЗА 7/8 — Обновление пакета до v1.1 (ЗАВЕРШЁН)

Причина: правка v1.1 (анти-деградация) в каноне — пакет содержал v1.0.
Регрессия после правки — прогон 5 (6/6 × 12/12), итого 30/30 ответов ≥ 11/12.

Изменено в пакете:
- промт v1.1: `v1_промт.md` + `prompts\dev.txt` (хэш `2B1DCCE6...`);
- скилы: dev-checks (`BE696D8A...`), dev-reflexion (`7B0DBC31...`),
  dev-registry (`9A515E07...`), dev-templates (`FF330540...`);
  dev-comparison и dev-guardrails не менялись (хэши прежние);
- `01_план\ОТЧЁТ_ТЕСТ.md` — прогоны 1–5;
- добавлен `ЖУРНАЛ_ОШИБОК.md` (3 записи, все «устранена»);
- обновлены: `СВОДКА_ХЭШЕЙ.md`, `РЕЗЮМЕ.md`, `РЕЛИЗ_СООБЩЕНИЕ.md`,
  `00_ЧЕК_ЛИСТ_УСТАНОВКИ.md`, `ЧЕК_ЛИСТ_СБОРКИ.md`.
- конфиг `opencode_developer.jsonc` не менялся (хэш `465861F8...`).
- добавлен `00_РЕЕСТР_СКИЛОВ.md` (версии + changelog, хэш `A16C2A15...`);
  ZIP пересобран (20 записей, 56.5 KB).

Сверка с каноном по SHA-256: 13/13 OK. ZIP `Разработчик_v1.1_пакет.zip` собран.

## Дата: 2026-09-19

### ФАЗА 0 — Окружение (ЗАВЕРШЁН)
Установлены: Git 2.55.0, Python 3.13.15 + 3.11.9 (через py-лаунчер),
VS Code 1.138.0 (+ ms-python.python, vscode-pylance, ms-vscode.powershell),
GitHub CLI 2.101.0 (portable в «Установлено ИИ»).
Уже было: Node 24.20 + npx, uv 0.12.17, winget.
Манифест и установщики: `Рабочая папка ИИ\Установлено ИИ\УСТАНОВЛЕНО.md`.
Изменения системы: PATH (git, python, code) — виден в новых сессиях.

### ФАЗА 1 — Реструктуризация родительского проекта (ЗАВЕРШЁН)
- `проект промт` разделён на `01_учитель` + `02_разработчик`.
- Общие MCP (`02_mcp_готовые`) и правила (`00_ПРАВИЛА_ПРОЕКТА.md`) в корне.
- Учительские скилы → глобальная папка `~/.config/opencode/skills`
  (7: 6 учительских + officecli). skills.paths убран из конфига.
- Регрессия учителя — после перезапуска opencode (зафиксировано в журнале учителя).

### ФАЗА 2 — Каркас (ЗАВЕРШЁН)
- Создана структура: `01_план`, `02_промт` (+`prompts`), `03_скилы`, `04_коллегам`.
- README, ЖУРНАЛ, план тестирования, реестр скилов.

### ФАЗА 3 — Промт v1.0 (ЗАВЕРШЁН)
- Создан `02_промт\v1_промт.md` (8 блоков, зеркало v6: контекст → роль →
  протокол 5 шагов → направления → запреты → взаимодействие/адаптивность →
  арсенал → инициализация). Объём ~18 KB (~4–5K токенов — целевой лёгкий).
- Синхронная копия `02_промт\prompts\dev.txt`.
- Хэш всех 3 копий (канон/dev.txt/глобальная): `DE29787F4C18D26E...`.

### ФАЗА 4 — Скилы (ЗАВЕРШЁН)
Создано 6 dev-скилов (канон `03_скилы\`, скопированы в глобальную
`~/.config/opencode/skills`, хэши совпали 6/6):
dev-registry · dev-guardrails · dev-templates (Шаблон 5 — блочная работа) ·
dev-comparison (Memory-of-Thought) · dev-checks (41 проверка) · dev-reflexion
(самоанализ + правила 19–20 + протокол роста).

### ФАЗА 5 — Агент developer (ЗАВЕРШЁН)
- В глобальном `opencode.jsonc` добавлен агент `developer` (mode primary,
  prompt `{file:./prompts/dev.txt}`, temperature 0.2). JSON валиден.
- `prompts\dev.txt` скопирован в глобальную папку промтов. 3 копии синхронны.

### ФАЗА 6 — Тест (ЗАВЕРШЁН)
- Перезапуск opencode выполнен → dev-скилы и агент `developer` подхвачены
  (проверено: skill dev-registry загружен из глобальной папки). Хэши 3 копий
  промта и 6 скилов совпали до начала прогона.
- **Прогон 1 (свежая сессия): 6/6 запросов по 12/12** — порог ≥11/12 пройден.
  Отчёт: `01_план\ОТЧЁТ_ТЕСТ.md` (baseline для правила 20).
- **ПРАВКА (после прогона 1):** команда «тест разработчика» не распознавалась
  в свежей сессии (агент отвечал «готов к работе»). Зарегистрирована в промте
  (арсенал + инициализация) и добавлен раздел 4 «Протокол прогона тестов»
  в dev-reflexion. Новые хэши: промт `7BEDEF5E...`, dev-reflexion `30931CCF...`.
  По правилу 19 — прогон 2 считается регрессией после правки.
- **Прогоны 2 и 3 (свежие сессии): 6/6 × 12/12 каждый** — итого 18/18 ответов
  ≥ 11/12, среднее 12.0. Порог приёмки преодолён, деградации нет.
  Отчёт: `01_план\ОТЧЁТ_ТЕСТ.md` (прогоны 1–3, baseline подтверждён).

### ФАЗА 7 — Пакет для коллег (ЗАВЕРШЁН)
- Собрано в `04_коллегам\`: `00_ЧЕК_ЛИСТ_УСТАНОВКИ.md`, `РЕЗЮМЕ.md`,
  `РЕЛИЗ_СООБЩЕНИЕ.md`, `СВОДКА_ХЭШЕЙ.md`, `opencode_developer.jsonc`,
  `v1_промт.md` + `prompts\dev.txt`, `03_скилы\` (6 скилов), `01_план\`,
  `ЖУРНАЛ.md`.
- Копии в пакете сверены с каноном по SHA-256: 12/12 OK.
- Конфиг `opencode_developer.jsonc` — хэш `465861F8...`.

### ФАЗА 8 — ZIP (ЗАВЕРШЁН)
- `02_разработчик\Разработчик_v1.0_пакет.zip` (47.7 KB, 19 записей) —
  собран после 100% тестов (правило ZIP в `00_ПРАВИЛА_ПРОЕКТА.md`).
- Регрессия учителя (отдельно, 6 запросов) — при необходимости отдельным
  прогоном в подпроекте `01_учитель`.