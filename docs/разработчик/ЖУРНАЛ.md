# ЖУРНАЛ изменений — подпроект «02_разработчик»

Правила работы: блочная работа (Шаблон 5), регрессия после каждой правки
(правила 19–20), снапшоты перед авторегенерацией, синхронизация канон↔глобальная
папка по SHA-256. Полный свод — `00_ПРАВИЛА_ПРОЕКТА.md` (в корне проекта).

Источник вдохновения/эталон: подпроект `01_учитель` (промт v6, 6 скилов,
механика регрессии, чек-листы).

## Дата: 2026-09-20

### ПРАВКА (самоанализ → рост, правило 19)

Причина: самоанализ выявил слабости в исполнении (критерий 6 «Корректность
кода», 8/10): (а) inline-JSON в officecli batch ломался кавычками PowerShell —
2 ошибки до успеха; (б) был убит процесс officecli (`Stop-Process`), принятый
за зависший resident, — это MCP-сервер, инструмент `officecli_officecli`
пропал до перезапуска opencode. Вывод: слабость в исполнении = промт не давал
инструкций по работе с Office-документами (направление отсутствовало).

Изменено:
- промт v1.0: добавлено направление работы 7 «Office-документы (docx/xlsx/pptx)»
  — officecli (MCP или CLI); при блокировке файла сначала `close`, затем
  проверка процессов (POWERPNT/WINWORD/EXCEL); batch — JSON в файл + `--input`;
  запрет убивать officecli-процесс (MCP-сервер). Детали — dev-registry.
- dev-registry: officecli 1.0.151 добавлен в таблицу окружения; в «Известные
  ловушки по стеку» добавлен блок officecli (inline-JSON → файл + `--input`,
  close при блокировке, не убивать MCP-процесс).

Новые хэши (синхронны):
- промт: `DA8BABE72DD3F231E72CA1519E76EF6F78BB0619C666022B7622030D9F6FAE0C`
- dev-registry: `9A515E07A6B2B7D78DF3691905824CD4F66E9F2C1AF42CC304FE2684FC94C284`

Снапшоты до правки: `история_снапшоты\v1_промт_20260920_1118.md`,
`история_снапшоты\dev-registry_20260920_1118.md`.

По правилу 19 — требуется регрессия: прогон 4 в свежей сессии (после
перезапуска opencode, команда «тест разработчика»), порог ≥11/12.
Результат — в `01_план\ОТЧЁТ_ТЕСТ.md`.

### ПРОГОН 4 (регрессия, правило 19) — ЗАВЕРШЁН
2026-09-20, свежая сессия: 6/6 запросов × 12/12, порог ≥11/12 пройден,
деградации нет. Итого 24/24 ответов ≥ 11/12 (прогоны 1–4). Отчёт обновлён.

### ПРАВКА v1.1 (анти-деградация) — 2026-09-20
Причина (запрос пользователя): (1) зацикливание в длинных задачах — нужен
обязательный протокол ТЗ→блоки→цели→журнал по умолчанию; (2) те же правки
применимы к учителю; (3) уборка мусора после задач; (4) глобальный журнал
ошибок: фиксация по ходу работы + проверка в самоанализе + устранение,
автономно в каждом подпроекте (работает при передаче только одного агента).

Изменено:
- промт v1.0 → v1.1: Шаг 3 и Блок 6 — большая задача по умолчанию
  ТЗ→блоки→цели→журнал; запрет анти-зацикливания; уборка мусора (п.10);
  фиксация ошибок в ЖУРНАЛ_ОШИБОК.md (п.11); арсенал dev-reflexion.
- dev-templates Шаблон 5: этап 0 ТЗ, цели блоков, журнал задачи,
  анти-зацикливание.
- dev-reflexion: критерии 11–12 (журнал ошибок, уборка мусора), раздел 9
  (журнал ошибок / анти-зацикливание / уборка мусора).
- dev-checks: 41 → 42 (G6 — нет мусора).
- Создан ЖУРНАЛ_ОШИБОК.md (3 исторические записи).
- Зеркально у учителя: промт v6.1, lesson-templates, self-reflexion,
  quality-checks (F6), ЖУРНАЛ_ОШИБОК.md.

Снапшоты до правки: `история_снапшоты\*_20260920_1146.md` (8 файлов:
4 разработчика + 4 учителя).

Новые хэши (синхронны канон ↔ глобальная, 9/9 OK):
- промт v1.1: `2B1DCCE6DA6DA8C4FD2ED59E164E0A3E8C61C7CDE63E93D4F4721D78405C78C0`
- dev-templates: `FF33054054C33197BBA27469A004CC7E5B08AC7876D0F0101ED9C4552019BAC9`
- dev-reflexion: `7B0DBC31F9B312C633368EC637B24A10373C13E5FEAAEBB4B534114D235C6EA7`
- dev-checks: `BE696D8A5BEDF35B01C067F75DCD23A3737387C5A88BDF38EDCF44D2F7BA7AEB`

По правилу 19 — требуется регрессия: прогон 5 в свежей сессии (команда
«тест разработчика»), порог ≥11/12. Результат — в `01_план\ОТЧЁТ_ТЕСТ.md`.

### ПРОГОН 5 (регрессия, правило 19) — ЗАВЕРШЁН
2026-09-20, свежая сессия: 6/6 запросов × 12/12, порог ≥11/12 пройден,
деградации нет. Итого 30/30 ответов ≥ 11/12 (прогоны 1–5). Отчёт обновлён.
Замечание: пакет `04_коллегам` содержит промт v1.0 (`7BEDEF5E...`) —
требует обновления до v1.1 + изменённые скилы (dev-templates, dev-reflexion,
dev-checks), сверка SHA-256, пересборка ZIP (ФАЗА 7/8).

### ФАЗА 7/8 — Пакет v1.1 и ZIP (ЗАВЕРШЁН)
2026-09-20. Обновлён пакет `04_коллегам` до v1.1:
- промт v1.1 (`v1_промт.md` + `prompts\dev.txt`, хэш `2B1DCCE6...`);
- скилы: dev-checks `BE696D8A...`, dev-reflexion `7B0DBC31...`,
  dev-registry `9A515E07...`, dev-templates `FF330540...`
  (dev-comparison, dev-guardrails не менялись);
- `01_план\ОТЧЁТ_ТЕСТ.md` — прогоны 1–5;
- добавлен `ЖУРНАЛ_ОШИБОК.md` (3 записи, все «устранена»);
- обновлены: `СВОДКА_ХЭШЕЙ.md`, `РЕЗЮМЕ.md`, `РЕЛИЗ_СООБЩЕНИЕ.md`,
  `00_ЧЕК_ЛИСТ_УСТАНОВКИ.md`, `ЧЕК_ЛИСТ_СБОРКИ.md`, `ЖУРНАЛ.md`.
- конфиг `opencode_developer.jsonc` не менялся (`465861F8...`).
Сверка с каноном по SHA-256: 12/12 OK.
ZIP `Разработчик_v1.1_пакет.zip` (54.6 KB, 19 записей) — в `02_разработчик\`,
имена UTF-8 проверены. Старый `Разработчик_v1.0_пакет.zip` оставлен как снапшот.
Обновлён `00_РЕЕСТР_СКИЛОВ.md`: dev-registry v1.1, dev-templates v1.1,
dev-checks v1.1, dev-reflexion v1.2; dev-guardrails и dev-comparison — v1.0
(без изменений).
Дополнение: `00_РЕЕСТР_СКИЛОВ.md` добавлен в пакет `04_коллегам`
(хэш `A16C2A15...`), перечни содержимого и СВОДКА_ХЭШЕЙ.md обновлены,
ZIP пересобран: `Разработчик_v1.1_пакет.zip` (56.5 KB, 20 записей).
Сверка канон ↔ пакет: 13/13 OK.

Открытый пункт (TODO): пользователь хотел добавить ещё что-то глобально,
но забыл — допишет позже.
→ ЗАКРЫТ (2026-09-20): TODO — это и была текущая работа «обмен с коллегами
через GitHub» (репозитории opencode-kollegam, oshibki-kollegam,
proekt-promt, команда «поделиться промтом»). Выполнено, см. запись
«ПРАВКА (обмен с коллегами через GitHub, правило 19)» ниже.

### САМОАНАЛИЗ (2026-09-20, вечер) — dev-reflexion

Средний балл: **8.2 /10** (критерии 1–12: 9,9,8,8,8,8,8,8,7,9,8,8).
Сильно (≥8): 11 критериев (инструменты, безопасность, шаблоны, сравнение,
проверки, корректность, обучение, память, формат, журнал, уборка).
Слабо (<6): нет. Средние: п.9 «Противостояние деградации» (7).

Причина п.9: контекст — создан дублирующий репозиторий `opencode-kollegam`
без проверки существующего пакета `04_коллегам`; план тестирования искался
не в той папке. Зафиксировано в ЖУРНАЛ_ОШИБОК.md (открыта).

Решение: не править промт/скилы — слабость в контексте. Устранение:
1) объединить репозиторий `opencode-kollegam` с пакетом `04_коллегам` (v1.1);
2) синхронизировать канон ↔ пакет ↔ рабочую копию dev-reflexion (SHA-256);
3) регрессия (правило 19) после правки dev-reflexion — прогон 6 в свежей
сессии (команда «тест разработчика»), здесь — частичная проверка.

### ПРАВКА dev-reflexion (общая база ошибок коллег) — 2026-09-20
Причина: задача «обмен ошибками коллег» — устранённые ошибки переносятся
в общий репозиторий `oshibki-kollegam` (GitHub), в личном журнале остаются
только открытые.
Изменено: dev-reflexion раздел 9 — добавлен подраздел «Общая база ошибок
коллег (oshibki-kollegam)»: git pull, перенос по шаблону, удаление из
личного журнала, push, проверка обновлений комплекта.
Снапшот до правки: `история_снапшоты\dev-reflexion_20260920_1146.md`.
По правилу 19 — требуется регрессия: прогон 6 в свежей сессии.

## Дата: 2026-09-19

### ФАЗА 0 — Окружение (ЗАВЕРШЁН)
Установлены: Git 2.55.0, Python 3.13.15 + 3.11.9 (через py-лаунчер),
VS Code 1.138.0 (+ ms-python.python, vscode-pylance, ms-vscode.powershell),
GitHub CLI 2.101.0 (portable в «Установлено ИИ»).
Уже было: Node 24.20 + npx, uv 0.12.17, winget.
Манифест и установщики: `Рабочая папка ИИ\Установлено ИИ\УСТАНОВЛЕНО.md`.
Изменения системы: PATH (git, python, code) — виден в новых сессиях.

### ФАЗА 1 — Реструктуризация родительского проекта (ЗАВЕРШЁН)
- `проект промт` разделён на `01_учитель` + `02_разработчик`.
- Общие MCP (`02_mcp_готовые`) и правила (`00_ПРАВИЛА_ПРОЕКТА.md`) в корне.
- Учительские скилы → глобальная папка `~/.config/opencode/skills`
  (7: 6 учительских + officecli). skills.paths убран из конфига.
- Регрессия учителя — после перезапуска opencode (зафиксировано в журнале учителя).

### ФАЗА 2 — Каркас (ЗАВЕРШЁН)
- Создана структура: `01_план`, `02_промт` (+`prompts`), `03_скилы`, `04_коллегам`.
- README, ЖУРНАЛ, план тестирования, реестр скилов.

### ФАЗА 3 — Промт v1.0 (ЗАВЕРШЁН)
- Создан `02_промт\v1_промт.md` (8 блоков, зеркало v6: контекст → роль →
  протокол 5 шагов → направления → запреты → взаимодействие/адаптивность →
  арсенал → инициализация). Объём ~18 KB (~4–5K токенов — целевой лёгкий).
- Синхронная копия `02_промт\prompts\dev.txt`.
- Хэш всех 3 копий (канон/dev.txt/глобальная): `DE29787F4C18D26E...`.

### ФАЗА 4 — Скилы (ЗАВЕРШЁН)
Создано 6 dev-скилов (канон `03_скилы\`, скопированы в глобальную
`~/.config/opencode/skills`, хэши совпали 6/6):
dev-registry · dev-guardrails · dev-templates (Шаблон 5 — блочная работа) ·
dev-comparison (Memory-of-Thought) · dev-checks (41 проверка) · dev-reflexion
(самоанализ + правила 19–20 + протокол роста).

### ФАЗА 5 — Агент developer (ЗАВЕРШЁН)
- В глобальном `opencode.jsonc` добавлен агент `developer` (mode primary,
  prompt `{file:./prompts/dev.txt}`, temperature 0.2). JSON валиден.
- `prompts\dev.txt` скопирован в глобальную папку промтов. 3 копии синхронны.

### ФАЗА 6 — Тест (ЗАВЕРШЁН)
- Перезапуск opencode выполнен → dev-скилы и агент `developer` подхвачены
  (проверено: skill dev-registry загружен из глобальной папки). Хэши 3 копий
  промта и 6 скилов совпали до начала прогона.
- **Прогон 1 (свежая сессия): 6/6 запросов по 12/12** — порог ≥11/12 пройден.
  Отчёт: `01_план\ОТЧЁТ_ТЕСТ.md` (baseline для правила 20).
- **ПРАВКА (после прогона 1):** команда «тест разработчика» не распознавалась
  в свежей сессии (агент отвечал «готов к работе»). Зарегистрирована в промте
  (арсенал + инициализация) и добавлен раздел 4 «Протокол прогона тестов»
  в dev-reflexion. Новые хэши: промт `7BEDEF5E...`, dev-reflexion `30931CCF...`.
  По правилу 19 — прогон 2 считается регрессией после правки.
- **Прогоны 2 и 3 (свежие сессии): 6/6 × 12/12 каждый** — итого 18/18 ответов
  ≥ 11/12, среднее 12.0. Порог приёмки преодолён, деградации нет.
  Отчёт: `01_план\ОТЧЁТ_ТЕСТ.md` (прогоны 1–3, baseline подтверждён).

### ФАЗА 7 — Пакет для коллег (ЗАВЕРШЁН)
- Собрано в `04_коллегам\`: `00_ЧЕК_ЛИСТ_УСТАНОВКИ.md`, `РЕЗЮМЕ.md`,
  `РЕЛИЗ_СООБЩЕНИЕ.md`, `СВОДКА_ХЭШЕЙ.md`, `opencode_developer.jsonc`,
  `v1_промт.md` + `prompts\dev.txt`, `03_скилы\` (6 скилов), `01_план\`,
  `ЖУРНАЛ.md`.
- Копии в пакете сверены с каноном по SHA-256: 12/12 OK.
- Конфиг `opencode_developer.jsonc` — хэш `465861F8...`.

### ФАЗА 8 — ZIP (ЗАВЕРШЁН)
- `02_разработчик\Разработчик_v1.0_пакет.zip` (47.7 KB, 19 записей) —
  собран после 100% тестов (правило ZIP в `00_ПРАВИЛА_ПРОЕКТА.md`).
- Регрессия учителя (отдельно, 6 запросов) — при необходимости отдельным
  прогоном в подпроекте `01_учитель`.

### ПРАВКА (обмен с коллегами через GitHub, правило 19)

Причина: пользователь попросил перенести проект на GitHub, чтобы коллеги
(работают удалённо) могли устанавливать комплект по ссылке и обмениваться
ошибками; оба агента (учитель + разработчик) должны ставиться вместе.

Изменено:
- промт v1.1: добавлена команда «поделиться промтом» / «поделиться»
  (инициализация, п.7) — выдаёт короткий текст с описанием, установкой,
  функциями и ссылкой на GitHub.
- install.ps1: копирование скилов папками (`имя\SKILL.md`), проверка
  обновления комплекта (git pull), инструкция «установка по ссылке» в шапке.
- Репозитории GitHub (аккаунт KrivolapovDani):
  - `opencode-kollegam` (PUBLIC) — единый комплект: промты dev.txt + v6.txt,
    13 скилов, конфиги обоих агентов, install.ps1, документация;
  - `oshibki-kollegam` (PUBLIC) — общая база ошибок коллег;
  - `proekt-promt` (PRIVATE) — весь проект (01_учитель, 02_разработчик,
    02_mcp_готовые, 00_ПРАВИЛА_ПРОЕКТА.md), ZIP-артефакты исключены.

Новые хэши (синхронны, 6 копий dev.txt):
- промт: `7105CFF6623F8775E98971EDD0A0F2302BEC316E5A2F5E3A1940BDE6BC32B257`
- v6.txt (5 копий): `C20EC72FD6EE3E6F993B9E9B5E8791D1BEB612F54931585DD0D9CB028F5E9B66`

По правилу 19 — требуется регрессия: прогон 6 в свежей сессии (после
перезапуска opencode, команда «тест разработчика»), порог ≥11/12.
Результат — в `01_план\ОТЧЁТ_ТЕСТ.md`.

### ПРОГОН 6 (регрессия, правило 19) — ЗАВЕРШЁН

Дата: 2026-09-20 (вечер) · Сессия: свежая (команда «тест разработчика»).
Хэши: промт `7105CFF6...` (4 копии синхронны), скилы 6/6 OK
(dev-reflexion `07523643...` — обновлён). Канон ↔ рабочая копия синхронны.
Результат: **6/6 запросов × 12/12** — порог ≥11/12 пройден, деградации нет.
Итого 36/36 ответов ≥ 11/12 (прогоны 1–6).
При сверке хэшей выявлено: правки промта (15:45) и dev-reflexion (15:09)
были выполнены после прогона 5 (11:57) без регрессии — нарушение правила 19;
прогон 6 закрыл его задним числом. СВОДКА_ХЭШЕЙ.md обновлена.
ЖУРНАЛ_ОШИБОК.md: ошибка «дублирующий репозиторий opencode-kollegam»
закрыта (репозиторий объединён с пакетом 04_коллегам, v1.1) и перенесена
в общую базу `oshibki-kollegam` вместе с 3 ранее устранёнными ошибками.

### ПРАВКА: отмена ZIP-сборки, передача через git (2026-09-20, вечер)

Причина: проект передаётся как команда автоустановки в opencode через
публичный репозиторий `opencode-kollegam`; ZIP-сборка больше не нужна.

Изменено:
- `00_ПРАВИЛА_ПРОЕКТА.md` (раздел 5) и корневой `README.md`: правило
  «ZIP создаётся после 100% тестов» заменено на git-передачу
  (команда «Установи комплект по ссылке …» / install.ps1 из репозитория).
- Промты dev (5 копий) и v6 (6 копий): текст «поделиться» упрощён —
  убрано «скачайте ZIP и запустите install.ps1», осталась git-ссылка.
- `04_коллегам\ЧЕК_ЛИСТ_СБОРКИ.md` → `ЧЕК_ЛИСТ_ПЕРЕДАЧИ.md` (git-передача).
- dev-reflexion (раздел 4): поиск плана тестирования ограничен каноническим
  проектом `проект промт` (репозиторий proekt-promt) — НЕ по всему компьютеру
  (исправление замечания пользователя к прогону 6).
- ОТЧЁТ_ТЕСТ.md (канон + пакет): «следующий шаг» — синхронизация с
  репозиторием вместо пересборки ZIP.

По правилу 19 — требуется регрессия: прогон 7 в свежей сессии
(команда «тест разработчика»), порог ≥11/12.